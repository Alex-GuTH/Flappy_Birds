name: Deploy Pygame Web to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build tool
        run: |
          python -m pip install --upgrade pip
          pip install pygbag

      - name: Build web version
        run: python -m pygbag --build main.py

      - name: Resolve output directory
        shell: bash
        run: |
          if [ -d "build/web" ]; then
            echo "WEB_DIR=build/web" >> "$GITHUB_ENV"
          elif [ -d "build" ]; then
            INDEX_PATH=$(find build -name "index.html" | head -n 1)
            if [ -z "$INDEX_PATH" ]; then
              echo "No index.html found under build output"
              exit 1
            fi
            echo "WEB_DIR=$(dirname "$INDEX_PATH")" >> "$GITHUB_ENV"
          else
            echo "Build output directory not found"
            exit 1
          fi

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ env.WEB_DIR }}

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
